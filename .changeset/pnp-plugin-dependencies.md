---
"@zorilla/puppeteer-extra": patch
"@zorilla/puppeteer-extra-plugin": patch
---

Resolve auto-loaded plugin dependencies from the plugin that declared them so Yarn Plug'n'Play can load nested stealth dependencies without requiring extra top-level peer installs.
